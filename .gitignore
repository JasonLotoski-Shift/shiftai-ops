# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env — broader than the Next.js default to catch .env / .env.development too, since this is a private app repo and every env file we add will hold secrets.
.env*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

# Prototype worker run artifacts (per-run HTML + screenshots)
/worker/.runs/
